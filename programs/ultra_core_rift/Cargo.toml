[package]
name = "ultra_core_rift"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = { version = "0.30.0", features = ["init-if-needed"] }
rift-common = { path = "../../crates/rift-common" }

[dev-dependencies]
proptest = "1.0"
