[workspace]

[package]
license-file = "../../LICENSE"
publish = false
name = "rift-property-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-lang = { version = "0.30.0", features = ["init-if-needed"] }
ultra_core_rift = { path = "../../programs/ultra_core_rift" }

[dev-dependencies]
proptest = "1.0"
